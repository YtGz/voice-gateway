{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Wakeword Configuration",
  "description": "Configuration for a wakeword character folder",
  "type": "object",
  "properties": {
    "wakewords": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "List of wakeword phrases that trigger this character (for documentation)"
    },
    "threshold": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "default": 0.5,
      "description": "Detection threshold (0.0-1.0)"
    },
    "model": {
      "type": "string",
      "default": "model.onnx",
      "description": "Path to the ONNX or TFLite model file relative to this folder"
    },
    "porcupine_model": {
      "type": "string",
      "description": "Path to the Porcupine .ppn model file relative to this folder (optional)"
    }
  },
  "required": ["wakewords"]
}
