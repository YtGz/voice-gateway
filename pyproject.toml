[project]
name = "voice-gateway"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openwakeword>=0.4.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
    "sounddevice>=0.5.3",
]
